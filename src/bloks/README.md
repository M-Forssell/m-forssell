# Bloks

Storyblok content blocks. Each file maps 1:1 to a content type in the CMS.

These receive a `blok` prop with CMS data and are rendered by `StoryblokServerComponent`. They are registered in `src/lib/storyblok.ts`.

See [ARCHITECTURE.md](../../ARCHITECTURE.md) for the full bloks vs components distinction.
