# Foundation

Design tokens and SCSS utilities shared across all components.

- `color.scss` — Color palette and theme variables (CSS custom properties)
- `typography.scss` — Font sizes and fluid typography mixins
- `breakpoints.scss` — Responsive breakpoint mixins
- `device-width.scss` — Device width constants
